# iReader-Zhengyue-USBDebug
With the latest iReader updates, custom apps can't be deployed via proxy. This is a collection of scripts that crashes the iReader app to access the Android settings menu.
